services:
  admin-agentx:
    image: ghcr.io/abilex-gmbh/admin-agentx:latest
    ports:
      - '${PORT:-3000}:${PORT:-3000}'
    restart: unless-stopped
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    env_file: .env
    environment:
      - PORT=${PORT:-3000}
